import { ImagenAleatoriaPipe } from './imagen-aleatoria.pipe';

describe('ImagenAleatoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagenAleatoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
